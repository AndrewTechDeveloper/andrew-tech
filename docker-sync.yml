version: '2'
syncs:
  api-sync:
    src: './api/'
    notify_terminal: true
    sync_strategy: 'unison'
    sync_userid: '1000'
    sync_excludes: ['.git', '.gitignore', 'package.json']
  frontend-sync:
    src: './frontend/'
    notify_terminal: true
    sync_strategy: 'unison'
    sync_userid: '2000'
    sync_excludes: ['node_modules', '.git', '.gitignore']
