version: '2'
options:
  verbose: true
syncs:
  app-sync:
    src: '.'
    notify_terminal: true
    sync_strategy: 'native_osx'
    sync_userid: '2000'
    sync_excludes: ['.git', '.gitignore', 'node_modules']
